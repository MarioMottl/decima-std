main :: () {
    x := 99

    for (i: int = 0; i < 3; i += 1) {
        y: int = 5
    }

    print(i)  // 3  ← leaked from the loop-init
    print(y)  // 5  ← leaked from the loop-body
    print(x)  // 99
}

