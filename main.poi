import std.core as core
import std.math as math
import std.string as string

main :: () {
    print(math::PI)

    parts := string::split_at_whitespace("! mann")
    for (i: int = 0; i < len(parts); i += 1) {
        print(parts[i])
    }
}

